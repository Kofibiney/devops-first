# GitHub Actions Deployment Workflow

This project demonstrates how to deploy a simple HTML file to GitHub Pages using GitHub Actions.

## How it works

Whenever the `index.html` file is changed in the `main` branch, a GitHub Actions workflow is triggered to deploy the file to GitHub Pages.
